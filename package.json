{
  "name": "purifycss_rails_sample",
  "version": "0.1.0",
  "description": "Run purifycss using grunt against a rails project's assets.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "grunt": "^1.0.1",
    "grunt-purifycss": "^0.1.2"
  }
}
